NODE_ENV=development

# Supabase (wired in next phase)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Groq — server-only. Used by the AI assistant for resume tailoring + cover
# letter drafting. Never expose this to the browser.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
