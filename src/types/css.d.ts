// Side-effect imports of global stylesheets (e.g. `import './globals.css'`).
// Next.js handles these at build time; this shim only exists so the IDE's TS
// server doesn't flag the import.
declare module '*.css';
